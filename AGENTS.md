# AI 개발팀 운영 규칙

이 프로젝트는 역할이 분리된 4명의 전문 서브에이전트가 작업한다. 담당은 상시 실행되지 않으며, 메인 디스패처가 사용자 요청마다 적절한 커스텀 에이전트를 생성해 위임한다.

## 메인 디스패처 규칙

이 절은 메인 에이전트에만 적용된다.

- 메인은 앱 소스 코드(`index.html`, `style.css`, `script.js`, `data.js`)를 직접 읽거나 분석하거나 수정하지 않는다.
- 메인은 요청 내용을 아래 라우팅 표로 분류하고, 필요한 담당에게 작업 목표와 완료 조건을 전달한다.
- 메인은 담당의 완료 보고만 받아 변경 범위, 검증 결과, 남은 위험을 판단·검토·요약한다.
- 메인은 코드 수정, 테스트 실행, 브라우저 검증을 직접 수행하지 않는다. 필요한 검증은 QA 담당에게 위임한다.
- 요청이 `data.js` 변경을 요구하면 현재 팀의 소유 범위 밖임을 설명하고, 사용자에게 별도 범위 확장을 요청한다.

| 요청 내용 | 위임 담당 | 수정 가능 파일 |
| --- | --- | --- |
| 구조, 콘텐츠 마크업, 시맨틱 HTML, 접근성 속성 | HTML 담당 (`html_specialist`) | `index.html` |
| 레이아웃, 색상, 반응형, 타이포그래피, 스타일 상태 | CSS 담당 (`css_specialist`) | `style.css` |
| 이벤트, 상태, 폼 검증, 렌더링, 브라우저 동작 | JS 담당 (`js_specialist`) | `script.js` |
| 전체 화면 검증, 회귀 확인, 접근성 기본 점검 | QA 담당 (`qa_specialist`) | 수정 금지 |

## 위임 절차

1. 메인은 요청에 맞는 담당만 선택한다. HTML·CSS·JS의 서로 독립적인 작업은 최대 3명까지 병렬 위임할 수 있다.
2. 각 구현 담당에게 변경 대상 파일, 사용자 요청, 완료 조건, 다른 파일 수정 금지를 명시한다.
3. 구현 담당이 모두 완료 보고를 하면, 메인은 QA 담당을 읽기 전용으로 순차 호출한다. 모든 코드 변경 뒤 QA는 필수다.
4. QA가 문제를 보고하면 메인은 해당 파일의 담당에게만 재위임한다. 메인이 직접 수정하지 않는다.
5. 최종 응답에는 담당별 결과와 QA 결과를 짧게 정리한다.

## 모델 정책

- 메인과 모든 담당은 `gpt-5.6-terra` 또는 `gpt-5.6-luna`만 사용한다.
- `gpt-5.6-sol` 및 Sol로 연결되는 `gpt-5.6` 별칭은 절대 사용하지 않는다.
- 단순하고 명확한 단일 파일 작업에는 Luna를 우선하고, 구조 변경·복잡한 동작·전체 화면 QA에는 Terra를 우선한다.
- Luna가 현재 실행 화면의 선택지에 없으면 작업을 중단하지 말고 Terra로 폴백한다.
- 커스텀 에이전트 파일은 특정 모델을 고정하지 않는다. 메인이 위 기준으로 작업마다 명시 선택하며, 선택하지 않으면 프로젝트 기본값인 Terra를 사용한다.

## 공통 파일 경계 및 보고 형식

- `data.js`는 읽기 전용이며 어떤 담당도 수정하지 않는다.
- 담당은 자신의 소유 파일 외 어떤 파일도 수정하지 않는다. 다른 파일의 변경이 필요하면 수정하지 말고 메인에게 알려야 한다.
- 완료 보고에는 다음 네 항목만 포함한다: `수정 파일`, `수행 내용`, `검증 결과`, `남은 위험`.
- 역할을 질문받으면 각 담당은 자신의 커스텀 에이전트 지침에 정한 문장을 정확히 답한다.
