# AI 개발팀 사용 안내

이 프로젝트에는 요청마다 생성되는 네 명의 전문 Codex 담당이 등록되어 있습니다. 메인은 코드를 직접 읽거나 고치지 않고, 요청을 담당에게 전달한 뒤 결과와 QA 보고를 정리합니다.

## 담당과 파일 경계

| 담당 | Codex 에이전트 이름 | 담당 파일 | 하는 일 |
| --- | --- | --- | --- |
| HTML 담당 | `html_specialist` | `index.html` | 화면 구조, 시맨틱 HTML, 접근성 속성, 콘텐츠 마크업 |
| CSS 담당 | `css_specialist` | `style.css` | 레이아웃, 반응형, 색상, 글꼴, 상태 스타일 |
| JS 담당 | `js_specialist` | `script.js` | 이벤트, 상태, 폼 검증, 렌더링, 브라우저 동작 |
| QA 담당 | `qa_specialist` | 수정 없음 | 전체 화면과 동작을 읽기 전용으로 검증 |

`data.js`는 현재 수정 금지 파일입니다. 이 파일의 데이터를 바꾸려면 먼저 담당 범위를 확장해 달라고 요청하세요.

## 모델 선택 규칙

- 사용할 수 있는 모델은 `gpt-5.6-terra`와 `gpt-5.6-luna`뿐입니다.
- Sol 및 `gpt-5.6` 별칭은 사용하지 않습니다.
- 단순한 단일 파일 작업은 Luna를 우선하고, 복잡한 변경·전체 화면 QA는 Terra를 우선합니다.
- Luna가 선택 목록에 없으면 Terra가 자동 대안입니다.

## 바로 쓸 수 있는 요청 예시

- `HTML 담당에게 헤더 구조를 수정해 줘.`
- `CSS 담당에게 모바일 여백을 조정해 줘.`
- `JS 담당에게 문의 폼 검증을 수정해 줘.`
- `QA 담당에게 전체 화면을 검증해 줘.`
- `이 변경을 담당별로 위임하고, 마지막에 QA까지 진행해 줘.`

더 구체적으로 요청하려면 완료 기준도 함께 적으세요. 예: `CSS 담당에게 760px 이하에서 버튼이 한 줄에 보이도록 조정해 줘. 다른 파일은 수정하지 마.`

## 작업 흐름

1. 메인이 요청을 HTML, CSS, JS, QA 작업으로 나눕니다.
2. 독립적인 HTML·CSS·JS 작업은 최대 3명까지 동시에 진행합니다.
3. 구현 담당의 결과가 모이면 QA 담당이 전체 화면을 검증합니다.
4. QA가 문제를 찾으면 메인이 해당 파일 담당에게만 다시 맡깁니다.

각 담당은 완료 시 `수정 파일`, `수행 내용`, `검증 결과`, `남은 위험`을 보고합니다.
